Template.registerHelper('getSetting', function (setting) {
  return getSetting(setting);
});

// numBitsInInt = 30;

// getBitMask = function(numLocations, locations) {
// 	var numIntsInArray = (numLocations + numBitsInInt - 1) / numBitsInInt;
// 	numIntsInArray = Math.floor(numIntsInArray);
	
// }